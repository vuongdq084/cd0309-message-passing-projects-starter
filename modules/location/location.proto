syntax = "proto3";
import "google/protobuf/timestamp.proto";

message LocalMessage {
  int32 person_id = 1;
  string longitude = 2;
  string latitude = 3;
  google.protobuf.Timestamp creation_time = 4;
}

service LocalService {
  rpc Create(LocalMessage) returns (LocalMessage);
}